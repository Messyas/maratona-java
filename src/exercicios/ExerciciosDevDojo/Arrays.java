package exercicios.ExerciciosDevDojo;

/**
 * Entenda o conceito de arrays em Java:
 *
 * Familiarize-se com o conceito de arrays em Java, que são estruturas de dados que armazenam coleções de elementos do mesmo tipo.
 * Leia a documentação oficial da Oracle sobre arrays em Java.
 * Declarando e inicializando arrays:
 *
 * Aprenda a declarar e inicializar arrays em Java.
 * Experimente criar arrays de diferentes tipos de dados (por exemplo, int, double, String).
 * Acesso aos elementos do array:
 *
 * Aprenda a acessar elementos individuais em um array usando índices.
 * Entenda como os índices funcionam em Java (começando em 0).
 * Iterando sobre arrays:
 *
 * Explore diferentes formas de iterar sobre os elementos de um array, como loops for, loops for-each e loops while.
 * Arrays multidimensionais:
 *
 * Aprenda a trabalhar com arrays multidimensionais em Java (por exemplo, matrizes).
 * Experimente criar e manipular matrizes bidimensionais e tridimensionais.
 * Passagem de arrays para métodos:
 *
 * Entenda como os arrays são passados para métodos em Java.
 * Pratique passar arrays como argumentos para métodos e manipulá-los dentro dos métodos.
 * Modificação de arrays:
 *
 * Experimente modificar elementos de arrays.
 * Pratique adicionar, remover e modificar elementos em arrays.
 * Arrays de objetos:
 *
 * Aprenda a criar arrays de objetos em Java.
 * Experimente criar arrays de objetos de diferentes classes e manipulá-los.
 * Referências e cópias de arrays:
 *
 * Entenda a diferença entre referências e cópias de arrays em Java.
 * Pratique criar referências para arrays existentes e fazer cópias de arrays.
 * Utilização das classes utilitárias do Java para manipulação de arrays:
 *
 * Explore as classes utilitárias do Java, como Arrays, para realizar operações comuns em arrays, como ordenação, pesquisa e preenchimento.
 * Exercícios práticos:
 *
 * Realize exercícios práticos para aplicar o que aprendeu sobre arrays em Java.
 * Crie programas que envolvam a manipulação de arrays para resolver problemas do mundo real.
 * Revisão e prática contínua:
 *
 * Revise regularmente os conceitos aprendidos sobre arrays em Java.
 * Continue praticando e explorando diferentes maneiras de usar arrays em seus programas.
 */
public class Arrays {
    public static void main(String[] args) {
        String[] nome = new String[10];
        //quais sao os metodos de insercao em arrays?
        // como inserir no final e retirar um objeto do array
        // como inserior no meio
        //
    }
}
